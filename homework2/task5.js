let isAdult = Numberprompt("how old are you");
if (isAdult >= 18) {
  alert("You have reached the age of majority");
} else if (isAdult < 18) {
  alert("You are still too young");
} else {
  alert("please try again");
}
